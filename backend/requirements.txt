# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Vector store & search ---
chromadb==0.6.3
rank-bm25==0.2.2

# --- LLM / embeddings runtime ---
ollama==0.4.5

# --- Document loaders ---
pypdf==5.1.0
python-docx==1.1.2
ebooklib==0.18
beautifulsoup4==4.12.3
lxml==5.3.0
# Native PDF table extraction + vision-ingest image handling
pdfplumber==0.11.10
pymupdf==1.28.2
pillow==12.3.0

# --- Text processing ---
tiktoken==0.8.0

# --- Testing ---
pytest==9.1.1
hypothesis==6.168.0

# Note: httpx is a transitive dependency of both chromadb and ollama.
# We intentionally do NOT pin it so pip can resolve a version compatible
# with both (ollama requires httpx<0.28,>=0.27).
